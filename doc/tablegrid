/**
 * Props summary:
 * - columns: [{ key, title, width, render, searchable, filterable }]
 * - data: array of rows (objects)
 * - totalCount: number (for server-side pagination) optional
 * - pageSizeOptions: [10,25,50]
 * - paginationMode: 'client' | 'server'
 * - page: current page (1-based) for controlled use
 * - pageSize: current page size for controlled use
 * - onPageChange(page) => void
 * - onPageSizeChange(size) => void
 * - onSearch({ key, value }) => void
 * - onFilter({ key, value }) => void
 * - renderNoData => node
 * - className => string
 */
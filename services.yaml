apiVersion: v1
kind: Service
metadata:
  name: cspm-ui
  namespace: cspm-ui
spec:
  selector:
    app: cspm-ui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
